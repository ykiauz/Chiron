version: 1.0
provider:
  name: openfaas
  gateway: http://127.0.0.1:8080
functions:
  upload-media:
    lang: python3-flask
    handler: ./upload-media
    image: upload-media:latest
    labels:
      "com.openfaas.scale.max": "1"
